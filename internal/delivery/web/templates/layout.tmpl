<!doctype html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <title>{{ block "title" . }}Dataset Hub{{ end }}</title>
  <link rel="stylesheet" href="{{$.BasePath}}/static/css/main.css">
</head>
<body>
<header>
  <h1>Dataset Hub</h1>
  <nav>
    <a href="{{$.BasePath}}/categories">Категории</a> |
    <a href="{{$.BasePath}}/datasets">Датасеты</a> |
    {{ if or (eq .Role "user") (eq .Role "admin") }}
      <a href="{{$.BasePath}}/my-datasets">Мои датасеты</a> |
      <a href="{{$.BasePath}}/profile">Личный кабинет</a> |
    {{ end }}
    {{ if or (eq .Role "user") (eq .Role "admin") }}
      <a href="{{$.BasePath}}/notifications">Уведомления</a> |
    {{ end }}
    {{ if eq .Role "guest" }}
      <a href="{{$.BasePath}}/login">Вход</a>
    {{ else }}
      <form action="{{$.BasePath}}/logout" method="post" style="display:inline;">
        <button type="submit">Выйти</button>
      </form>
    {{ end }}
  </nav>
  <hr>
</header>

<main>
  {{ block "content" . }}{{ end }}
</main>

<footer>
  <hr>
  <small>&copy; 2025 Dataset Hub</small>
</footer>
</body>
</html>
