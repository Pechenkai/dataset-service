{{ define "title" }}Список публичных датасетов{{ end }}

{{ define "content" }}
<h2>Публичные датасеты</h2>

<table class="striped">
  <thead>
    <tr>
      <th>ID</th>
      <th>Название</th>
      <th>Описание</th>
      <th>Владелец</th>
      <th>Действия</th>
    </tr>
  </thead>
  <tbody>
    {{ range .Datasets }}
      <tr>
        <td>{{ .ID }}</td>
        <td><a href="/datasets/{{ .ID }}">{{ .Name }}</a></td>
        <td>{{ .Description }}</td>
        <td>{{ .OwnerUsername }}</td>
        <td>
          {{/* Если ты авторизован (роль user/admin), можно сразу иметь иконку «Добавить в избранное» или т. п. */}}
          —
        </td>
      </tr>
    {{ end }}
  </tbody>
</table>

{{/* Если ты зарегистрирован(а), показать ссылку на «Мои датасеты» */}}
{{ if or (eq .Role "user") (eq .Role "admin") }}
  <p><a href="/my-datasets">Мои датасеты</a></p>
{{ end }}
{{ end }}